[] 未附带PIN码时显示的配对的错误页面显示不友好，应该改为PIN码输入界面
[] 不要答题后立即关闭答题页面，因为用户可能要看解析
[] 工具调用后前端不要立刻滚动到工具调用部分，因为用户可能仍在查看输出内容
[] config.yml
```
当前上下文限制：{max_context}K tokens
    Token使用情况：{token_count} / {token_threshold}
	```
可能导致上下文缓存失效。应该尽量保证前缀一致。
[] 即使没有开启联网，依然提供了联网工具，需要修复。

**请用git提交**